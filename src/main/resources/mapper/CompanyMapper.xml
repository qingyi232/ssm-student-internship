<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.internship.mapper.CompanyMapper">

    <resultMap id="BaseResultMap" type="Company">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="contact_person" property="contactPerson"/>
        <result column="contact_phone" property="contactPhone"/>
        <result column="address" property="address"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findById" resultMap="BaseResultMap">
        SELECT id, name, contact_person, contact_phone, address, description, status, create_time, update_time
        FROM company
        WHERE id = #{id}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT id, name, contact_person, contact_phone, address, description, status, create_time, update_time
        FROM company
    </select>

    <select id="findByCondition" resultMap="BaseResultMap">
        SELECT id, name, contact_person, contact_phone, address, description, status, create_time, update_time
        FROM company
        <where>
            <if test="name != null and name != ''">
                AND name LIKE <![CDATA[ CONCAT('%', #{name}, '%') ]]>
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO company (name, contact_person, contact_phone, address, description, status, create_time, update_time)
        VALUES (#{name}, #{contactPerson}, #{contactPhone}, #{address}, #{description}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="update">
        UPDATE company
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="contactPerson != null">contact_person = #{contactPerson},</if>
            <if test="contactPhone != null">contact_phone = #{contactPhone},</if>
            <if test="address != null">address = #{address},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM company WHERE id = #{id}
    </delete>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM company
    </select>

</mapper>
