<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.internship.mapper.AnnouncementMapper">

    <resultMap id="BaseResultMap" type="Announcement">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="publisher_id" property="publisherId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="publisher_name" property="publisherName"/>
    </resultMap>

    <sql id="Base_Column_List">
        a.id, a.title, a.content, a.publisher_id, a.status, a.create_time, a.update_time,
        u.real_name as publisher_name
    </sql>

    <sql id="Announcement_Join">
        FROM announcement a
        LEFT JOIN sys_user u ON a.publisher_id = u.id
    </sql>

    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        <include refid="Announcement_Join"/>
        WHERE a.id = #{id}
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        <include refid="Announcement_Join"/>
        ORDER BY a.create_time DESC
    </select>

    <select id="findByStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        <include refid="Announcement_Join"/>
        WHERE a.status = #{status}
        ORDER BY a.create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO announcement (title, content, publisher_id, status, create_time, update_time)
        VALUES (#{title}, #{content}, #{publisherId}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="update">
        UPDATE announcement
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="publisherId != null">publisher_id = #{publisherId},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM announcement WHERE id = #{id}
    </delete>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM announcement
    </select>

</mapper>
